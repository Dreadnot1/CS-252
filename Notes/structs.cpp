#include <iostream>
#include <vector>
#include <map>
#include <string>

using namespace std;

// Similar to classes but can't use methods, all members are public by default
// Good for efficient use of small, frequently used data
struct Color {
    int red;
    int green;
    int blue;
};


int main() {
    Color lBlue = {173, 216, 230}
    
    return 0;
}